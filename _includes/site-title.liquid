<header id="header">
 <a href="{{ site.url }}{{ site.data.locales[page.locale].blog.site_url }}" class="logo">
 {% if page.locale %}
  {{ site.data.locales[page.locale].blog.title }}
 {% else %}
  {{ site.title }}
 {% endif %}
 </a>
</header>
